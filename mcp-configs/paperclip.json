{
  "mcpServers": {
    "paperclip-api": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "env": {
        "PAPERCLIP_BASE_URL": "http://127.0.0.1:3100/api",
        "PAPERCLIP_API_KEY": "<your-board-api-key>"
      },
      "description": "Paperclip AI REST API — manage companies, agents, issues, budgets, and org charts. Use the OpenAPI spec at /api/openapi.json for endpoint discovery."
    }
  }
}
